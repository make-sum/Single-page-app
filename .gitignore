# Include your project-specific ignores in this file
# Read about how to use .gitignore: https://help.github.com/articles/ignoring-files
# local env files and build outputs
.env
.env.*
*.env
*.env.*

# dependency/build caches (never commit)
node_modules/
.pnpm-store/
.yarn/cache/
.cache/
.next/
coverage/
# keep these built artifacts out of future commits (but we did not purge their history)
dist/
build/
